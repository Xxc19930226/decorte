CREATE TABLE redirect_url (id BIGINT AUTO_INCREMENT, source VARCHAR(255) NOT NULL, destination VARCHAR(255) NOT NULL, created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL, UNIQUE INDEX source_idx (source), PRIMARY KEY(id)) DEFAULT CHARACTER SET UTF8 COLLATE utf8_unicode_ci ENGINE = INNODB;
